# JobFit-AI
This is a personal-use project that extracts job postings from LinkedIn, compares them with my profile, and returns the best-matching opportunities in ranked order.

## Progress so far:

- Developed a pipeline to scrape LinkedIn job postings and use an LLM to extract structured features from job descriptions; currently extending it with resume-to-job match scoring.
